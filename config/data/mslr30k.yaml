defaults:
  - base

_target_: src.data.MSLR30K
name: MSLR-Web30K
fold: 1
load_features: false
n_results: 201 # 90th percentile

pipeline:
  _target_: src.data.preprocessing.Pipeline
  steps:
    - _target_: src.data.preprocessing.StratifiedTruncate
      max_length: "${data.n_results}"
      random_state: "${random_state}"
    - _target_: src.data.preprocessing.DiscardShortQueries
      min_length: "${data.n_results}"
    - _target_: src.data.preprocessing.Shuffle
      random_state: "${random_state}"
    - _target_: src.data.preprocessing.GenerateDocumentIds
